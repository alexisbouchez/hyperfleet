name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run ESLint
        run: bun run lint

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run TypeScript type checking
        run: bun run typecheck

  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run unit tests
        run: bun run test:unit

  docker-integration-tests:
    name: Docker Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Pre-pull Docker images
        run: docker pull alpine:3.19

      - name: Run Docker integration tests
        run: bun run test:integration:docker
        env:
          # Increase timeout for Docker operations
          DOCKER_TEST_TIMEOUT: 120000

  api-integration-tests:
    name: API Integration Tests
    runs-on: ubuntu-latest
    needs: [unit-tests] # Run after unit tests pass
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Pre-pull Docker images
        run: docker pull alpine:3.19

      - name: Run API integration tests
        run: bun run test:integration:api
        env:
          DOCKER_TEST_TIMEOUT: 120000

  # VM Runtime Tests (Firecracker, Cloud Hypervisor)
  # These require KVM access and run on self-hosted runners or special GitHub runners
  # Uncomment when self-hosted runner with KVM is available
  #
  # vm-integration-tests:
  #   name: VM Integration Tests
  #   runs-on: [self-hosted, linux, kvm]
  #   needs: [unit-tests]
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - uses: oven-sh/setup-bun@v2
  #       with:
  #         bun-version: latest
  #
  #     - name: Install dependencies
  #       run: bun install --frozen-lockfile
  #
  #     - name: Setup VM runtimes
  #       run: bun run setup
  #
  #     - name: Run VM integration tests
  #       run: bun run test:integration:vm
